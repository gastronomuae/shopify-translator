import type {  ShopifyResourceStatus,  ShopifySyncProductRow,  ShopifySyncResourceType,} from "@/types";import { SHOPIFY_ADMIN_API_VERSION } from "@/lib/shopifyAdminApiVersion";import { normalizeNestedProductSyncTypes } from "@/lib/nestedProductSyncTypes";export { normalizeNestedProductSyncTypes };function parseProductResourceStatus(raw: string | undefined | null): ShopifyResourceStatus | undefined {  if (raw === "ACTIVE" || raw === "DRAFT" || raw === "ARCHIVED") return raw;  return undefined;}const API_VERSION = SHOPIFY_ADMIN_API_VERSION;/** Resources per single GraphQL document (aliased translatableResource fields). */export const SYNC_BATCH_RESOURCE_COUNT = 8;/** Parallel batch GraphQL requests (each batch has SYNC_BATCH_RESOURCE_COUNT resources). */export const SYNC_PARALLEL_BATCHES = 4;/** Products per products-connection page (Shopify max 250 ΓÇö fewer list round-trips). */export const SYNC_PRODUCTS_PAGE_SIZE = 250;/** In-memory session cache (per server instance). */const syncCache = new Map<string, { expires: number; products: ShopifySyncProductRow[] }>();export const SYNC_CACHE_TTL_MS = 60_000;export function getCachedSync(cacheKey: string): ShopifySyncProductRow[] | null {  const hit = syncCache.get(cacheKey);  if (!hit || hit.expires < Date.now()) {    if (hit) syncCache.delete(cacheKey);    return null;  }  return hit.products;}export function setCachedSync(cacheKey: string, products: ShopifySyncProductRow[]): void {  syncCache.set(cacheKey, { expires: Date.now() + SYNC_CACHE_TTL_MS, products });}const FIELD_KEYS = {  title: "title",  body: "body_html",  product_type: "product_type",} as const;/** * Shopify Admin `translatableResource.translatableContent` keys for product SEO. * (Internal app fields remain `meta_title` / `meta_description` in CSV + UI.) */const SHOPIFY_SEO_KEYS = {  title: "seo.title",  description: "seo.description",} as const;interface TranslatableContentItem {  key: string;  value: string;  locale: string;  /** Shopify TranslatableContentType, e.g. "SINGLE_LINE_TEXT_FIELD", "RICH_TEXT_FIELD". */  type?: string;  /** SHA-256 digest of the source value ΓÇö used to register translations and for backup restore. */  digest?: string;}interface TranslationItem {  key: string;  value: string | null;  /** True when the source content changed since the translation was registered. */  outdated?: boolean;}interface GqlExtensions {  cost?: {    requestedQueryCost?: number;    actualQueryCost?: number;    throttleStatus?: {      maximumAvailable?: number;      currentlyAvailable?: number;      restoreRate?: number;    };  };}/** * Valid values for Shopify Admin API 2025-07 MetafieldOwnerType enum. * Baseline = confirmed working on all stores. * Candidates = probed at sync start; skipped if the API returns an enum error. */type MetafieldOwnerType =  // Baseline ΓÇö always included  | "PRODUCT"  | "COLLECTION"  | "ARTICLE"  | "BLOG"  | "PAGE"  | "SHOP"  | "PRODUCTVARIANT"  // Candidates ΓÇö probed at runtime (valid on 2025-07, may return 0 definitions for some stores)  | "COMPANY"  | "COMPANY_LOCATION"  | "CUSTOMER"  | "LOCATION"  | "MARKET"  | "ORDER";/** Confirmed working on every store ΓÇö always loaded when includeMetafields=true. */const BASELINE_METAFIELD_OWNER_TYPES: MetafieldOwnerType[] = [  "PRODUCT",  "COLLECTION",  "ARTICLE",  "BLOG",  "PAGE",  "SHOP",  "PRODUCTVARIANT",];/** * New owner types to probe before use. * Only added to the definition-name loading pass if the probe returns ok/no_records. * Probe results are cached module-level (per API version) to avoid re-probing on every sync. */const CANDIDATE_METAFIELD_OWNER_TYPES: MetafieldOwnerType[] = [  "COMPANY",  "COMPANY_LOCATION",  "CUSTOMER",  "LOCATION",  "MARKET",  "ORDER",];/** * Module-level cache: enum validity is per-API-version, not per-store. * true  = valid enum + metafieldDefinitions query succeeds * false = enum_error or unexpected failure */const _ownerTypeProbeCache = new Map<MetafieldOwnerType, boolean>();interface ProductLocaleFields {  title: string;  body_html: string;  meta_title: string;  meta_description: string;  product_type: string;  /** Translatable metafields discovered at sync time (key = "meta.namespace.field"). */  metafields?: Record<string, string>;  /** Shopify TranslatableContentType per metafield key (used to determine FieldType). */  metafieldTypes?: Record<string, string>;  /** Optional UI metadata per metafield key (meta.namespace.key). */  metafieldMeta?: Record<    string,    {      id: string;      displayName: string;      namespaceKey: string;      parentTitle: string;      parentType: "PRODUCT" | "COLLECTION";    }  >;}function localeMatches(a: string, b: string): boolean {  return a.toLowerCase() === b.toLowerCase();}/** Map a Shopify TranslatableContentType string to our internal FieldType. */function shopifyTypeToFieldType(shopifyType: string | undefined): import("@/types").FieldType {  if (shopifyType === "RICH_TEXT_FIELD") return "html";  // ShopPolicy body and other HTML fields use type "HTML" in translatableContent.  if (shopifyType === "HTML") return "html";  return "plain";}/** Any of these keys (with a locale) can contribute to source-locale discovery ΓÇö not only title/body. */const SOURCE_LOCALE_SCAN_KEYS = new Set<string>([  FIELD_KEYS.title,  FIELD_KEYS.body,  SHOPIFY_SEO_KEYS.title,  SHOPIFY_SEO_KEYS.description,  /** Legacy key names if present */  "meta_title",  "meta_description",  FIELD_KEYS.product_type,  /** PRODUCT_OPTION / PRODUCT_OPTION_VALUE only have a "name" key ΓÇö include it so   *  resolveSourceLocale can discover the locale from option records. */  "name",]);function orderedUniqueLocalesForTitleBody(content: TranslatableContentItem[]): string[] {  const seen = new Set<string>();  const ordered: string[] = [];  for (const c of content) {    if (!SOURCE_LOCALE_SCAN_KEYS.has(c.key) && !c.key.startsWith("meta.")) continue;    const k = c.locale.toLowerCase();    if (seen.has(k)) continue;    seen.add(k);    ordered.push(c.locale);  }  return ordered;}function resolveSourceLocale(  content: TranslatableContentItem[],  preferred: string): { resolved: string; usedFallback: boolean } {  const locales = orderedUniqueLocalesForTitleBody(content);  if (locales.length === 0) {    return { resolved: preferred, usedFallback: false };  }  const hit = locales.find((l) => localeMatches(l, preferred));  if (hit !== undefined) {    return { resolved: hit, usedFallback: false };  }  return { resolved: locales[0], usedFallback: true };}function pickFieldsFromTranslatableContent(  content: TranslatableContentItem[],  locale: string): ProductLocaleFields {  const pick = (key: string) =>    content.find((c) => c.key === key && localeMatches(c.locale, locale))?.value ?? "";  /** Prefer Shopify SEO keys; fall back to legacy `meta_*` if API ever returns them. */  const pickSeoTitle = () => pick(SHOPIFY_SEO_KEYS.title) || pick("meta_title");  const pickSeoDescription = () => pick(SHOPIFY_SEO_KEYS.description) || pick("meta_description");  // Collect all translatable metafields (key starts with "meta.")  const metafields: Record<string, string> = {};  const metafieldTypes: Record<string, string> = {};  for (const c of content) {    if (!c.key.startsWith("meta.")) continue;    if (!localeMatches(c.locale, locale)) continue;    if (c.value) {      metafields[c.key] = c.value;      if (c.type) metafieldTypes[c.key] = c.type;    }  }  return {    title: pick(FIELD_KEYS.title),    body_html: pick(FIELD_KEYS.body),    meta_title: pickSeoTitle(),    meta_description: pickSeoDescription(),    product_type: pick(FIELD_KEYS.product_type),    ...(Object.keys(metafields).length > 0 ? { metafields, metafieldTypes } : {}),  };}function fieldsFromTargetTranslationsOnly(translations: TranslationItem[]): ProductLocaleFields {  const pick = (key: string) => {    const v = translations.find((t) => t.key === key)?.value;    return v ?? "";  };  const pickSeoTitle = () => pick(SHOPIFY_SEO_KEYS.title) || pick("meta_title");  const pickSeoDescription = () => pick(SHOPIFY_SEO_KEYS.description) || pick("meta_description");  // Collect translated metafields  const metafields: Record<string, string> = {};  for (const t of translations) {    if (t.key.startsWith("meta.") && t.value) {      metafields[t.key] = t.value;    }  }  return {    title: pick(FIELD_KEYS.title),    body_html: pick(FIELD_KEYS.body),    meta_title: pickSeoTitle(),    meta_description: pickSeoDescription(),    product_type: pick(FIELD_KEYS.product_type),    ...(Object.keys(metafields).length > 0 ? { metafields } : {}),  };}/** * Shopify includes these keys in translatableContent for some resource types, * but they are auto-generated by Shopify (e.g. the canonical URL for a policy) * and are not meaningful to translate. Returning "" causes buildDynamicFieldBag * to skip the field entirely. */const NON_TRANSLATABLE_KEYS = new Set(["url"]);function normalizeFieldKeyForUi(key: string): string {  if (key === "seo.title") return "meta_title";  if (key === "seo.description") return "meta_description";  // Return empty string so the caller's `if (!k) continue` skips this key.  if (NON_TRANSLATABLE_KEYS.has(key)) return "";  return key;}/** * Key prefixes that belong to Shopify Plus-only features. * These strings are Shopify-controlled and cannot be overridden on Basic/Standard plans. * Excluded at sync time for ALL stores ΓÇö this app targets non-Plus merchants. * * Shopify docs: https://shopify.dev/docs/apps/build/online-store/theme-translations */export const SHOPIFY_PLUS_ONLY_PREFIXES = [  "shopify.checkout.",  "shopify.checkpoint.",  "shopify.subscriptions.",  "shopify.feed.",] as const;/** * Reason categories for excluding a theme string from translation. * Kept as a string union so counts can be accumulated and logged. */type ThemeExclusionReason =  | "plus_only"    // Shopify Plus-only namespace (shopify.checkout.*, etc.)  | "empty"        // null, undefined, blank, single-char  | "color_hex"    // #fff, #ff0000, #aabbcc  | "color_fn"     // rgb(...), rgba(...), hsl(...), hsla(...)  | "url"          // https://..., http://...  | "url_proto"    // //cdn.shopify.com/...  | "liquid"       // {{ var }}, {% tag %}  | "boolean"      // true / false  | "number"       // 123, 12.5  | "css_dim"      // 16px, 1.5rem, 50%, "0 0 8px", "16px 24px"  | "icon_class"   // fa fa-..., bi bi-..., material-icons  | "image_path"   // .png, .jpg, .gif, .svg, .webp  | "asset_path";  // .css, .js, .scss, .less/** * Returns "plus_only" if the theme field key belongs to a Shopify Plus-only namespace, * or null if the key is allowed for all plan tiers. * This is a key-based check, independent of the field's value. */function themePlusOnlyKeyReason(key: string): "plus_only" | null {  for (const prefix of SHOPIFY_PLUS_ONLY_PREFIXES) {    if (key.startsWith(prefix)) return "plus_only";  }  return null;}/** * Returns the exclusion reason if the theme string should NOT be translated, * or null if it should be included (i.e. it is human-readable UI text). * * Γ£à Included: labels, button copy, availability messages, plain text * Γ¥î Excluded: hex/rgb/hsl colors, URLs, Liquid snippets, numbers/dims, *              booleans, icon classes, image/asset paths, empty strings */function themeValueExclusionReason(value: string | null | undefined): ThemeExclusionReason | null {  if (!value || typeof value !== "string") return "empty";  const v = value.trim();  if (v.length === 0 || v.length === 1) return "empty";  // Colors  if (v.startsWith("#")) return "color_hex";  if (/^rgba?\s*\(/i.test(v)) return "color_fn";  if (/^hsla?\s*\(/i.test(v)) return "color_fn";  // URLs  if (/^https?:\/\//i.test(v)) return "url";  if (v.startsWith("//")) return "url_proto";  // Liquid template code  if (/\{\{|\{%/.test(v)) return "liquid";  // Booleans  if (/^(true|false)$/i.test(v)) return "boolean";  // Pure numbers (integer or decimal)  if (/^\d+(\.\d+)?$/.test(v)) return "number";  // CSS dimensions ΓÇö single value: 16px, 1.5rem, 50%, 2em, 100vh, 0.5fr ΓÇª  const CSS_UNIT = /^(\d+(\.\d+)?)(px|rem|em|vh|vw|vmin|vmax|%|pt|cm|mm|ex|ch|fr)$/i;  if (CSS_UNIT.test(v)) return "css_dim";  // CSS shorthand: 2ΓÇô4 space-separated tokens where each is a number or dimension  // e.g. "0 0 8px", "16px 24px", "0 auto", "1px solid"? ΓÇö only pure dimension tokens  const CSS_TOKEN = /^\d+(\.\d+)?(px|rem|em|vh|vw|vmin|vmax|%|pt|cm|mm|ex|ch|fr)?$/i;  const dimParts = v.split(/\s+/);  if (    dimParts.length >= 2 &&    dimParts.length <= 4 &&    dimParts.every((p) => CSS_TOKEN.test(p))  ) {    return "css_dim";  }  // Icon classes (Font Awesome, Bootstrap Icons, Material)  if (v.startsWith("fa ") || v.startsWith("fa-") || v.startsWith("bi ") || v.startsWith("bi-")) return "icon_class";  if (/^material-icons/.test(v)) return "icon_class";  // Image file paths  if (/\.(png|jpg|jpeg|gif|svg|webp|ico)(\?.*)?$/i.test(v)) return "image_path";  // Asset file paths (CSS/JS bundles, SCSS)  if (/\.(css|js|mjs|scss|less|ts)(\?.*)?$/i.test(v)) return "asset_path";  return null; // include ΓÇö value appears to be human-readable text}/** Convenience wrapper for boolean use-sites. */function isTranslatableThemeValue(value: string | null | undefined): boolean {  return themeValueExclusionReason(value) === null;}function buildDynamicFieldBag(  content: TranslatableContentItem[],  translations: TranslationItem[],  sourceLocale: string): Record<string, { ru_content: string; en_content: string; fieldType: import("@/types").FieldType }> {  const byKey: Record<string, { ru_content: string; en_content: string; fieldType: import("@/types").FieldType }> = {};  for (const c of content) {    // Shopify returns an empty locale string for primary-locale content on some resource types    // (e.g. PRODUCT_OPTION, PRODUCT_OPTION_VALUE). An empty locale = primary locale = source.    // Only skip if locale is explicitly set to something OTHER than the source locale.    const hasExplicitLocale = c.locale.trim().length > 0;    if (hasExplicitLocale && !localeMatches(c.locale, sourceLocale)) continue;    const k = normalizeFieldKeyForUi(c.key);    if (!k) continue;    byKey[k] = {      ru_content: c.value ?? "",      en_content: byKey[k]?.en_content ?? "",      fieldType: shopifyTypeToFieldType(c.type),    };  }  for (const t of translations) {    const k = normalizeFieldKeyForUi(t.key);    if (!k) continue;    if (!(k in byKey)) {      byKey[k] = {        ru_content: "",        en_content: t.value ?? "",        fieldType: "plain",      };      continue;    }    byKey[k].en_content = t.value ?? "";  }  return byKey;}function toLegacySyncedProductRow(  handle: string,  shopify_id: string,  source: ProductLocaleFields,  target: ProductLocaleFields,  used_fallback: boolean,  resolved_source_locale: string,  resourceStatus: ShopifyResourceStatus | undefined,  publishedAt: string | null | undefined): ShopifySyncProductRow {  // Standard scalar fields only (exclude the metafield bags)  const SCALAR_KEYS = ["title", "body_html", "meta_title", "meta_description", "product_type"] as const;  const standardFieldEntries = Object.fromEntries(    SCALAR_KEYS.map((key) => [      key,      {        ru_content: source[key] ?? "",        en_content: target[key] ?? "",      },    ])  );  // Metafields ΓÇö carry fieldType so the mapper renders them correctly  const metaFieldEntries: Record<    string,    {      ru_content: string;      en_content: string;      fieldType: import("@/types").FieldType;      displayName?: string;      namespaceKey?: string;      metafieldId?: string;      parentTitle?: string;      parentType?: string;    }  > = {};  for (const [mk, mv] of Object.entries(source.metafields ?? {})) {    const meta = source.metafieldMeta?.[mk];    metaFieldEntries[mk] = {      ru_content: mv ?? "",      en_content: target.metafields?.[mk] ?? "",      fieldType: shopifyTypeToFieldType(source.metafieldTypes?.[mk]),      ...(meta        ? {            displayName: meta.displayName,            namespaceKey: meta.namespaceKey,            metafieldId: meta.id,            parentTitle: meta.parentTitle,            parentType: meta.parentType,          }        : {}),    };  }  const fields = { ...standardFieldEntries, ...metaFieldEntries };  return {    fields,    handle,    shopify_id,    ru_title: source.title,    ru_body: source.body_html,    en_title: target.title,    en_body: target.body_html,    ru_meta_title: source.meta_title,    en_meta_title: target.meta_title,    ru_meta_description: source.meta_description,    en_meta_description: target.meta_description,    ru_product_type: source.product_type,    en_product_type: target.product_type,    used_fallback,    resolved_source_locale,    fallback_reason: used_fallback ? "missing_source_locale" : null,    ...(resourceStatus !== undefined ? { resourceStatus } : {}),    ...(publishedAt !== undefined ? { publishedAt } : {}),  };}function gidToNumericId(gid: string): string {  const m = /\/(\d+)\s*$/.exec(gid);  return m ? m[1] : gid;}/** * Fetches all themes via REST and returns a map of numeric id ΓåÆ theme name. * Used to give ONLINE_STORE_THEME rows a human-readable title like "Prestige". */async function fetchThemeNameMap(  domain: string,  token: string): Promise<Map<string, string>> {  const url = `https://${domain}/admin/api/${API_VERSION}/themes.json`;  console.log(`[sync/theme] fetchThemeNameMap ΓåÆ GET ${url}`);  try {    const res = await fetch(url, {      headers: { "X-Shopify-Access-Token": token },    });    if (!res.ok) {      const body = await res.text().catch(() => "(unreadable)");      if (res.status === 403) {        console.warn(          `[sync/theme] themes.json HTTP 403 ΓÇö read_themes scope missing. ` +          `Re-authenticate the store to grant this scope. Theme display names will fall back to IDs until then.`,        );      } else {        console.warn(`[sync/theme] themes.json HTTP ${res.status}: ${body.slice(0, 300)}`);      }      return new Map();    }    const json = (await res.json()) as {      themes?: Array<{ id: number | string; name: string; role?: string }>;    };    const map = new Map<string, string>();    for (const t of json.themes ?? []) {      map.set(String(t.id), t.name);    }    console.log(`[sync/theme] themeNameMap (${map.size} entries):`, Object.fromEntries(map));    return map;  } catch (err) {    console.warn("[sync/theme] fetchThemeNameMap failed:", err instanceof Error ? err.message : err);    return new Map();  }}// ΓöÇΓöÇ Menu parent-map helpers ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇconst MENU_TREE_QUERY = `  query GetMenuTree {    menus(first: 250) {      nodes {        id        handle        title        items {          id          items {            id            items {              id            }          }        }      }    }  }`;interface _MenuTreeItem { id: string; items?: _MenuTreeItem[] }interface _MenuTreeNode { id: string; handle: string; title: string; items?: _MenuTreeItem[] }export interface LinkParentInfo {  menuId: string;  menuHandle: string;  menuTitle: string;  depth: number;  parentLinkId?: string;  index: number;}/** Returned by fetchMenuMaps ΓÇö both maps keyed by numeric Shopify ID. */interface MenuMaps {  /** numeric link ID ΓåÆ parent menu info (for enriching LINK sync rows) */  linkParentMap: Map<string, LinkParentInfo>;  /** numeric menu ID ΓåÆ { handle, title } (for enriching MENU sync rows) */  menuInfoMap: Map<string, { handle: string; title: string }>;}/** * Fetches all menus via the Navigation API and returns two maps: *  - `linkParentMap`: Link numeric-ID ΓåÆ { menuId, menuHandle, menuTitle, depth, ΓÇª } *  - `menuInfoMap`:  Menu numeric-ID ΓåÆ { handle, title } * * Both IDs (Navigation API and Translations API) share the same underlying * numeric IDs so these maps can enrich MENU and LINK sync rows. * * Requires `read_content` scope and API 2024-07+. On failure (e.g. "Access * denied" before a fresh OAuth), both maps are returned empty so sync continues * without parent info ΓÇö a warning is logged instead of throwing. */async function fetchMenuMaps(  domain: string,  token: string,): Promise<MenuMaps> {  const linkParentMap = new Map<string, LinkParentInfo>();  const menuInfoMap   = new Map<string, { handle: string; title: string }>();  try {    const { data } = await shopifyGqlWithExtensions(domain, token, MENU_TREE_QUERY);    const menus =      (data as { menus?: { nodes?: _MenuTreeNode[] } })?.menus?.nodes ?? [];    function walk(      items: _MenuTreeItem[],      menuId: string,      menuHandle: string,      menuTitle: string,      depth: number,      parentLinkId?: string,    ) {      items.forEach((item, idx) => {        const numId = gidToNumericId(item.id);        linkParentMap.set(numId, { menuId, menuHandle, menuTitle, depth, parentLinkId, index: idx });        if (item.items?.length) walk(item.items, menuId, menuHandle, menuTitle, depth + 1, numId);      });    }    for (const menu of menus) {      const numMenuId = gidToNumericId(menu.id);      menuInfoMap.set(numMenuId, { handle: menu.handle, title: menu.title });      walk(menu.items ?? [], numMenuId, menu.handle, menu.title, 0, undefined);    }    console.log(      `[sync/menus] fetched ${menus.length} menus, ${linkParentMap.size} link items`,    );  } catch (err) {    console.warn(      "[sync/menus] fetchMenuMaps failed ΓÇö MENU/LINK rows will have no parent context" +      " (likely missing read_content scope or pre-2024-07 API ΓÇö re-run OAuth if needed):",      err instanceof Error ? err.message : err,    );  }  return { linkParentMap, menuInfoMap };}// ΓöÇΓöÇ Shop policy title map ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇconst SHOP_POLICIES_QUERY = `  query GetShopPolicies {    shop {      shopPolicies {        id        title      }    }  }`;/** * Fetch all shop policies and return a GID ΓåÆ display title map. * Used during SHOP_POLICY sync so each policy record shows its human-readable * name (e.g. "Refund Policy") instead of the raw ID fallback. */async function fetchPolicyTitleMap(  domain: string,  token: string,): Promise<Map<string, string>> {  try {    const { data } = await shopifyGqlWithExtensions(domain, token, SHOP_POLICIES_QUERY);    const policies =      (data as { shop?: { shopPolicies?: Array<{ id: string; title: string }> } })        ?.shop?.shopPolicies ?? [];    // Diagnostic: log raw GIDs so we can verify format vs node.resourceId    console.info(      `[sync/SHOP_POLICY] shopPolicies raw (${policies.length}):`,      policies.map((p) => `id="${p.id}" title="${p.title}"`).join(" | "),    );    const map = new Map<string, string>();    for (const p of policies) {      if (!p.title) continue;      // Key by numeric ID (resilient to GID format differences).      // e.g. "gid://shopify/ShopPolicy/25094815913" ΓåÆ "25094815913"      const numericId = gidToNumericId(p.id ?? "");      if (numericId) map.set(numericId, p.title);      // Also store the full GID as a secondary key in case formats match exactly.      if (p.id) map.set(p.id, p.title);    }    console.info(      `[sync/SHOP_POLICY] policyTitleMap (${map.size / 2} policies):`,      Object.fromEntries([...map].filter(([k]) => !k.startsWith("gid://"))),    );    return map;  } catch (err) {    console.warn(      "[sync/SHOP_POLICY] fetchPolicyTitleMap failed ΓÇö policy names will fall back to IDs:",      err instanceof Error ? err.message : err,    );    return new Map();  }}// ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ/** Full GraphQL response including extensions (cost / throttle). */export async function shopifyGqlWithExtensions(  domain: string,  token: string,  query: string,  variables?: Record<string, unknown>): Promise<{ data: unknown; extensions?: GqlExtensions }> {  const res = await fetch(`https://${domain}/admin/api/${API_VERSION}/graphql.json`, {    method: "POST",    headers: {      "Content-Type": "application/json",      "X-Shopify-Access-Token": token,    },    body: JSON.stringify({ query, variables }),  });  const text = await res.text();  let json: {    data?: unknown;    errors?: { message: string }[];    extensions?: GqlExtensions;  };  try {    json = JSON.parse(text) as typeof json;  } catch {    throw new Error(`Shopify response not JSON (${res.status}): ${text.slice(0, 200)}`);  }  if (!res.ok) {    throw new Error(`Shopify HTTP ${res.status}: ${text.slice(0, 300)}`);  }  if (json.errors?.length) {    throw new Error(json.errors[0].message);  }  const ext = json.extensions;  if (process.env.NODE_ENV === "development" && ext?.cost) {    const c = ext.cost;    console.info("SHOPIFY_GQL_COST", {      requestedQueryCost: c.requestedQueryCost,      actualQueryCost: c.actualQueryCost,      throttle: c.throttleStatus,    });  }  return { data: json.data, extensions: ext };}/** Wait if throttle budget is low to reduce 429 risk. */export async function maybeThrottleAfterGql(extensions?: GqlExtensions): Promise<void> {  const avail = extensions?.cost?.throttleStatus?.currentlyAvailable;  if (avail === undefined) return;  if (avail < 200) {    const wait = Math.min(2000, 500 + (200 - avail) * 5);    await new Promise((r) => setTimeout(r, wait));  }}/** * GraphQL helper that does not throw on `errors` ΓÇö used to probe Admin enum support * for TranslatableResourceType (see `refineSyncTypesAfterTranslatableResourceProbe`). */async function shopifyGqlProbeRaw(  domain: string,  token: string,  query: string,): Promise<{  data: unknown;  errors?: ReadonlyArray<{ message: string; path?: ReadonlyArray<string | number> }>;}> {  const res = await fetch(`https://${domain}/admin/api/${API_VERSION}/graphql.json`, {    method: "POST",    headers: {      "Content-Type": "application/json",      "X-Shopify-Access-Token": token,    },    body: JSON.stringify({ query }),  });  const text = await res.text();  let json: {    data?: unknown;    errors?: ReadonlyArray<{ message: string; path?: ReadonlyArray<string | number> }>;  };  try {    json = JSON.parse(text) as typeof json;  } catch {    console.warn("[sync/translatable-resource-probe] Non-JSON response", text.slice(0, 200));    return { data: null, errors: [{ message: "Invalid JSON", path: [] }] };  }  return { data: json.data ?? null, errors: json.errors };}/** * Opt-in resource types that need an enum-support probe before syncing. * Each is tested independently so a failure on one never blocks the other. * PRODUCT_VARIANT is intentionally absent ΓÇö it is not a valid * TranslatableResourceType on Admin API 2025-07. */const PROBEABLE_OPT_IN_TYPES = ["PRODUCT_OPTION", "PRODUCT_OPTION_VALUE"] as const;/** * Probes each opt-in resource type independently with `first: 1`. * If Shopify returns a GraphQL enum/argument error for a type, it is removed * from syncTypes and a warning is logged ΓÇö other types are unaffected. * * Using a single combined query for multiple types is intentionally avoided: * an invalid enum in one alias causes the entire query to fail, which would * incorrectly strip valid types (the original bug with PRODUCT_VARIANT). */async function refineSyncTypesAfterTranslatableResourceProbe(  domain: string,  token: string,  syncTypes: ShopifySyncResourceType[],): Promise<ShopifySyncResourceType[]> {  const toProbe = (PROBEABLE_OPT_IN_TYPES as readonly string[]).filter((t) =>    syncTypes.includes(t as ShopifySyncResourceType),  );  if (toProbe.length === 0) return syncTypes;  let next = [...syncTypes];  for (const resourceType of toProbe) {    const probeQuery = `{ check: translatableResources(resourceType: ${resourceType}, first: 1) { nodes { resourceId } } }`;    const { data, errors } = await shopifyGqlProbeRaw(domain, token, probeQuery);    const hasError = !!errors?.length;    const hasData  = !!(data as { check?: { nodes?: unknown[] } } | null)?.check;    console.info(`[sync/probe] ${resourceType}: hasData=${hasData} errors=${JSON.stringify(errors ?? [])}`);    if (hasError && !hasData) {      console.warn(        `[sync/probe] ${resourceType} is not a valid TranslatableResourceType on API ${API_VERSION} ΓÇö skipping.`,      );      next = next.filter((t) => t !== resourceType);    }  }  return next;}function buildProductsPageQuery(queryFilter: string) {  return `  query ProductsForSync($first: Int!, $after: String) {    products(first: $first, after: $after, query: "${queryFilter}") {      pageInfo { hasNextPage endCursor }      edges { node { id handle status publishedAt } }    }  }`;}function buildProductsCountQuery(queryFilter: string) {  return `  query ProductsCountForSync {    productsCount(limit: null, query: "${queryFilter}") { count }  }`;}function buildTranslatableBatchQuery(n: number): string {  const varDecl = Array.from({ length: n }, (_, i) => `$id${i}: ID!`).join(", ");  const fields = Array.from({ length: n }, (_, i) => {    return `    r${i}: translatableResource(resourceId: $id${i}) {      translatableContent {        key        value        locale        type        digest      }      translations(locale: $targetLocale) {        key        value        outdated      }    }`;  }).join("\n");  return `    query BatchTranslatable($targetLocale: String!, ${varDecl}) {      ${fields}    }  `;}const TRANSLATABLE_RESOURCES_QUERY = `  query TranslatableResourcesForSync(    $resourceType: TranslatableResourceType!    $first: Int!    $after: String    $targetLocale: String!  ) {    translatableResources(resourceType: $resourceType, first: $first, after: $after) {      pageInfo {        hasNextPage        endCursor      }      nodes {        resourceId        translatableContent {          key          value          locale          type          digest        }        translations(locale: $targetLocale) {          key          value          outdated        }      }    }  }`;const TRANSLATABLE_RESOURCES_COUNT_QUERY = `  query TranslatableResourcesCountForSync($resourceType: TranslatableResourceType!) {    translatableResourcesCount(resourceType: $resourceType) {      count    }  }`;type TrNode = {  translatableContent: TranslatableContentItem[] | null;  translations: TranslationItem[] | null;} | null;export interface SyncEngineProgress {  processed: number;  total: number | null;  pct: number | null;}/** * Raw per-resource data collected during sync, passed to `onTypeComplete` for * backup capture.  Includes `digest` from `translatableContent` and `outdated` * from `translations` so callers can store a complete backup without extra GQL calls. */export interface SyncBackupRawItem {  /** Shopify GID, e.g. gid://shopify/Product/123 */  resourceId: string;  content: Array<{ key: string; digest?: string }>;  translations: Array<{ key: string; value: string | null; outdated?: boolean }>;}export interface RunShopifySyncParams {  domain: string;  token: string;  sourceLocale: string;  targetLocale: string;  onProgress?: (p: SyncEngineProgress) => void;  /**   * Sync only these resource kinds. Omit or `undefined` = default full sync (all kinds except   * opt-in PRODUCT_OPTION / PRODUCT_OPTION_VALUE, which must be sent explicitly).   * When set, kinds not listed are skipped (e.g. only `PRODUCT` runs the products pipeline).   */  types?: ShopifySyncResourceType[];  /**   * Called once per resource type after all its resources finish syncing.   * Receives the raw translatable content + translations for every resource of   * that type, enabling the caller to store backups from sync data without   * issuing additional GraphQL requests.   *   * Errors thrown by this callback are caught and logged ΓÇö they never abort   * the sync.   */  onTypeComplete?: (resourceType: string, items: SyncBackupRawItem[]) => Promise<void>;  /**   * Optional filter for the Shopify products query string.   * Example: "updated_at:>=2025-04-01T12:00:00Z" for incremental sync.   * Only affects the PRODUCT pipeline ΓÇö all other types run without this filter.   */  productQueryFilter?: string;}/** * Opt-in-only: synced only when the client includes these in `type` (see Sync toolbar * "Options & values"). Omitted when `type` is empty (server default full sync). */const SHOPIFY_SYNC_PRODUCT_OPTION_TYPES = ["PRODUCT_OPTION", "PRODUCT_OPTION_VALUE"] as const;/** * Default full sync when POST body omits `type` ΓÇö excludes product option/value (opt-in). * All entries must be valid TranslatableResourceType enum values on API 2025-07. *   PAGE / ARTICLE / BLOG replaced ONLINE_STORE_PAGE / _ARTICLE / _BLOG (confirmed 2025-07). *   SHOP_SMS_TEMPLATE replaces SMS_TEMPLATE ΓÇö probed at runtime and skipped if invalid. */const SHOPIFY_SYNC_DEFAULT_FULL_TYPES = [  "PRODUCT",  "COLLECTION",  "METAFIELD",  "METAOBJECT",  "PAGE",  "ARTICLE",  "BLOG",  "ONLINE_STORE_THEME",  "MENU",  "LINK",   // individual navigation links / menu items  "SHOP",  "SHOP_POLICY",  "DELIVERY_METHOD_DEFINITION",  "EMAIL_TEMPLATE",  "PAYMENT_GATEWAY",  "PACKING_SLIP_TEMPLATE",  // SMS_TEMPLATE / SHOP_SMS_TEMPLATE confirmed invalid on API 2025-07 ΓÇö removed  "SELLING_PLAN",  "SELLING_PLAN_GROUP",  "FILTER",] as const;/** All resource kinds this engine may sync when explicitly requested (includes opt-in kinds). */const SHOPIFY_SYNC_RESOURCE_TYPES = [  ...SHOPIFY_SYNC_DEFAULT_FULL_TYPES,  ...SHOPIFY_SYNC_PRODUCT_OPTION_TYPES,] as const;/** * Maps legacy / previously-stored type strings ΓåÆ canonical 2025-07 enum values. * Applied by parseSyncTypesQueryParam before any API call. * * Direction flipped from earlier: ONLINE_STORE_* were the canonical names before * 2025-07; now PAGE / ARTICLE / BLOG are the correct enum values. */const LEGACY_SYNC_TYPE_ALIASES: Record<string, ShopifySyncResourceType> = {  ONLINE_STORE_PAGE:    "PAGE",  ONLINE_STORE_BLOG:    "BLOG",  ONLINE_STORE_ARTICLE: "ARTICLE",  ONLINE_STORE_MENU:    "MENU",  // SMS_TEMPLATE / SHOP_SMS_TEMPLATE both confirmed invalid on 2025-07; no alias needed};const KNOWN_SYNC_RESOURCE_TYPES = new Set<ShopifySyncResourceType>([  ...SHOPIFY_SYNC_RESOURCE_TYPES,  // Legacy aliases ΓÇö accepted by parser, normalised to canonical before use  "ONLINE_STORE_PAGE",  "ONLINE_STORE_BLOG",  "ONLINE_STORE_ARTICLE",  "ONLINE_STORE_MENU",  "PRODUCT_VARIANT",] as ShopifySyncResourceType[]);export type ParseSyncTypesResult =  | { ok: true; types: ShopifySyncResourceType[] | undefined }  | { ok: false; error: string };/** * Parses `type` from POST /api/shopify/sync JSON body (comma-separated, case-insensitive). * Empty / missing = full sync (`types` undefined). Unknown tokens ΓåÆ error (for API 400). */export function parseSyncTypesQueryParam(raw: string | null): ParseSyncTypesResult {  if (raw == null || raw.trim() === "") {    return { ok: true, types: undefined };  }  const parts = raw    .split(",")    .map((s) => s.trim())    .filter(Boolean);  if (parts.length === 0) {    return { ok: true, types: undefined };  }  const seen = new Set<ShopifySyncResourceType>();  for (const p of parts) {    const u = p.toUpperCase();    const normalized = (LEGACY_SYNC_TYPE_ALIASES[u] ?? u) as ShopifySyncResourceType;    if (!KNOWN_SYNC_RESOURCE_TYPES.has(normalized)) {      return {        ok: false,        error: `Unknown sync type "${p}". Supported: ${[...KNOWN_SYNC_RESOURCE_TYPES].join(", ")}.`,      };    }    seen.add(normalized);  }  return { ok: true, types: [...seen] };}function shouldSyncProducts(types: ShopifySyncResourceType[] | undefined): boolean {  if (types === undefined || types.length === 0) return true;  return types.includes("PRODUCT");}function shouldSyncPages(types: ShopifySyncResourceType[] | undefined): boolean {  if (types === undefined || types.length === 0) return true;  // "PAGE" is the canonical 2025-07 enum; "ONLINE_STORE_PAGE" kept for backward compat  return types.includes("PAGE") || types.includes("ONLINE_STORE_PAGE");}const PAGES_PAGE_QUERY = `  query PagesForSync($first: Int!, $after: String) {    pages(first: $first, after: $after) {      pageInfo {        hasNextPage        endCursor      }      edges {        node {          id          handle        }      }    }  }`;function shouldSyncCollections(types: ShopifySyncResourceType[] | undefined): boolean {  if (types === undefined || types.length === 0) return true;  return types.includes("COLLECTION");}function shouldSyncBlogs(types: ShopifySyncResourceType[] | undefined): boolean {  if (types === undefined || types.length === 0) return true;  // "BLOG" is the canonical 2025-07 enum; "ONLINE_STORE_BLOG" kept for backward compat  return types.includes("BLOG") || types.includes("ONLINE_STORE_BLOG");}function shouldSyncArticles(types: ShopifySyncResourceType[] | undefined): boolean {  if (types === undefined || types.length === 0) return true;  // "ARTICLE" is the canonical 2025-07 enum; "ONLINE_STORE_ARTICLE" kept for backward compat  return types.includes("ARTICLE") || types.includes("ONLINE_STORE_ARTICLE");}/** METAOBJECT sync is opt-in ΓÇö not included in a default full sync. */function shouldSyncMetaobjects(types: ShopifySyncResourceType[] | undefined): boolean {  if (types === undefined || types.length === 0) return false;  return types.includes("METAOBJECT");}/** METAFIELD fetch is opt-in ΓÇö modifies product/collection queries to include metafields. */function shouldFetchMetafields(types: ShopifySyncResourceType[] | undefined): boolean {  if (types === undefined || types.length === 0) return false;  return types.includes("METAFIELD");}/** Reduced page size when metafields are fetched per-node (heavier queries). */const SYNC_PAGE_SIZE_WITH_METAFIELDS = 50;/** * Metafield types (lowercase, as returned by `metafields { nodes { type } }`) * that hold translatable text.  All other types ΓÇö json, boolean, references, * numeric, date, measurement, etc. ΓÇö are silently skipped during sync. * Applies identically across Product, Collection, Article, Page, Blog. */const TRANSLATABLE_METAFIELD_TYPES_LC = new Set([  "single_line_text_field",  "multi_line_text_field",  "rich_text_field",  "list.single_line_text_field",]);export interface DirectMetafield {  id: string;  namespace: string;  key: string;  type: string;  value: string | null;  definition?: { name?: string | null } | null;}const METAFIELD_DEFINITIONS_QUERY = `  query MetafieldDefinitionsForSync($ownerType: MetafieldOwnerType!, $first: Int!, $after: String) {    metafieldDefinitions(ownerType: $ownerType, first: $first, after: $after) {      pageInfo { hasNextPage endCursor }      nodes { name namespace key }    }  }`;/** Merge direct metafield values into a ProductLocaleFields source bag. Direct values win over translatableContent. */function mergeDirectMetafields(  source: ProductLocaleFields,  directMf: DirectMetafield[],  parentType: "PRODUCT" | "COLLECTION",  parentTitle: string,  definitionNameMap?: Map<string, string>): ProductLocaleFields {  const extra: Record<string, string> = {};  const extraTypes: Record<string, string> = {};  const extraMeta: NonNullable<ProductLocaleFields["metafieldMeta"]> = {};  for (const mf of directMf) {    if (!TRANSLATABLE_METAFIELD_TYPES_LC.has(mf.type)) continue;    if (!mf.value) continue;    const namespaceKey = `${mf.namespace}.${mf.key}`;    const metaKey = `meta.${namespaceKey}`;    extra[metaKey] = mf.value;    extraTypes[metaKey] = mf.type.toUpperCase();    extraMeta[metaKey] = {      id: mf.id,      displayName:        (mf.definition?.name ?? "").trim() ||        definitionNameMap?.get(namespaceKey) ||        namespaceKey,      namespaceKey,      parentTitle: parentTitle || "",      parentType,    };  }  if (Object.keys(extra).length === 0) return source;  return {    ...source,    metafields: { ...source.metafields, ...extra },    metafieldTypes: { ...source.metafieldTypes, ...extraTypes },    metafieldMeta: { ...source.metafieldMeta, ...extraMeta },  };}function createMetafieldRowsFromDirect(  directMf: DirectMetafield[],  parentType: "PRODUCT" | "COLLECTION",  parentTitle: string,  parentHandle: string,  sourceLocale: string,  targetByKey: Record<string, string> | undefined,  definitionNameMap?: Map<string, string>): ShopifySyncProductRow[] {  const rows: ShopifySyncProductRow[] = [];  for (const mf of directMf) {    if (!TRANSLATABLE_METAFIELD_TYPES_LC.has(mf.type)) continue;    if (!mf.value) continue;    const namespaceKey = `${mf.namespace}.${mf.key}`;    const displayName =      (mf.definition?.name ?? "").trim() ||      definitionNameMap?.get(namespaceKey) ||      namespaceKey;    const target = targetByKey?.[`meta.${namespaceKey}`] ?? "";    rows.push({      resourceKind: "METAFIELD",      handle: `${parentHandle}/${namespaceKey}`,      shopify_id: gidToNumericId(mf.id),      fields: {        value: {          ru_content: mf.value,          en_content: target,          fieldType: shopifyTypeToFieldType(mf.type.toUpperCase()),          displayName,          namespaceKey,          metafieldId: mf.id,          parentTitle,          parentType,        },      },      ru_title: displayName,      en_title: target,      ru_body: "",      en_body: "",      used_fallback: false,      resolved_source_locale: sourceLocale,      fallback_reason: null,    });  }  return rows;}function buildProductsPageWithMetafieldsQuery(queryFilter: string) {  return `  query ProductsForSyncWithMetafields($first: Int!, $after: String) {    products(first: $first, after: $after, query: "${queryFilter}") {      pageInfo { hasNextPage endCursor }      edges {        node {          id handle title status publishedAt          metafields(first: 50) {            nodes {              id              namespace              key              type              value              definition { name }            }          }        }      }    }  }`;}const COLLECTIONS_PAGE_WITH_METAFIELDS_QUERY = `  query CollectionsForSyncWithMetafields($first: Int!, $after: String) {    collections(first: $first, after: $after) {      pageInfo { hasNextPage endCursor }      edges {        node {          id handle title          metafields(first: 50) {            nodes {              id              namespace              key              type              value              definition { name }            }          }        }      }    }  }`;/** * Metaobject field types Shopify considers translatable text. * Values are lowercase as returned by `metaobjects { fields { type } }`. */const TRANSLATABLE_FIELD_TYPES = new Set([  "single_line_text_field",  "multi_line_text_field",  "rich_text_field",]);/** Step 1 ΓÇö discover all translatable metaobject types (paginated). */const METAOBJECT_DEFINITIONS_QUERY = `  query MetaobjectDefinitions($first: Int!, $after: String) {    metaobjectDefinitions(first: $first, after: $after) {      pageInfo { hasNextPage endCursor }      nodes {        type        name        capabilities {          translatable { enabled }        }      }    }  }`;/** Step 2 ΓÇö fetch instances with raw field values for a given type (paginated). */const METAOBJECTS_WITH_FIELDS_QUERY = `  query MetaobjectsWithFields($type: String!, $first: Int!, $after: String) {    metaobjects(type: $type, first: $first, after: $after) {      pageInfo { hasNextPage endCursor }      nodes {        id        handle        fields {          key          value          type        }      }    }  }`;function toMetaobjectSyncRow(  handle: string,  shopify_id: string,  moType: string,  fields: Record<string, { ru_content: string; en_content: string; fieldType: import("@/types").FieldType }>,  used_fallback: boolean,  resolved_source_locale: string): ShopifySyncProductRow {  const nameKey = ["name", "title"].find((k) => k in fields);  const ru_title = nameKey ? fields[nameKey].ru_content : handle;  const en_title = nameKey ? fields[nameKey].en_content : "";  return {    fields,    handle: `${moType}/${handle}`,    shopify_id,    ru_title,    en_title,    ru_body: "",    en_body: "",    used_fallback,    resolved_source_locale,    fallback_reason: used_fallback ? "missing_source_locale" : null,    resourceKind: "METAOBJECT",  };}const BLOGS_PAGE_QUERY = `  query BlogsForSync($first: Int!, $after: String) {    blogs(first: $first, after: $after) {      pageInfo { hasNextPage endCursor }      edges { node { id handle } }    }  }`;const ARTICLES_PAGE_QUERY = `  query ArticlesForSync($first: Int!, $after: String) {    articles(first: $first, after: $after) {      pageInfo { hasNextPage endCursor }      edges { node { id handle } }    }  }`;const COLLECTIONS_PAGE_QUERY = `  query CollectionsForSync($first: Int!, $after: String) {    collections(first: $first, after: $after) {      pageInfo {        hasNextPage        endCursor      }      edges {        node {          id          handle        }      }    }  }`;export async function runShopifySync(params: RunShopifySyncParams): Promise<ShopifySyncProductRow[]> {  const { domain, token, sourceLocale, targetLocale, onProgress, onTypeComplete } = params;  let syncTypes: ShopifySyncResourceType[] =    params.types === undefined || params.types.length === 0      ? [...SHOPIFY_SYNC_DEFAULT_FULL_TYPES]      : normalizeNestedProductSyncTypes([...params.types]);  syncTypes = await refineSyncTypesAfterTranslatableResourceProbe(domain, token, syncTypes);  const products: ShopifySyncProductRow[] = [];  const includeMetafields = shouldFetchMetafields(syncTypes);  /** Collects raw translatable content + translations per node; drained after each type. */  const typeRawBuffer: SyncBackupRawItem[] = [];  /**   * Drains `typeRawBuffer` and forwards to `onTypeComplete`.   * Errors are caught and logged so they never interrupt the sync.   */  async function fireTypeComplete(resourceType: string): Promise<void> {    const items = typeRawBuffer.splice(0);    if (!onTypeComplete) return;    try {      await onTypeComplete(resourceType, items);    } catch (e) {      console.warn(`[sync/backup] onTypeComplete error for ${resourceType}:`, e instanceof Error ? e.message : e);    }  }  // Build once at sync start and reuse for all resources:  // "namespace.key" -> "Human Name"  const definitionNameMap = new Map<string, string>();  let metafieldDefinitionEntriesLoaded = 0;  let metafieldRowsCreated = 0;  async function loadMetafieldDefinitionNames(ownerType: MetafieldOwnerType): Promise<void> {    let after: string | null = null;    let hasNext = true;    while (hasNext) {      const { data, extensions } = await shopifyGqlWithExtensions(domain, token, METAFIELD_DEFINITIONS_QUERY, {        ownerType,        first: 50,        after,      });      await maybeThrottleAfterGql(extensions);      const conn = (data as {        metafieldDefinitions?: {          pageInfo: { hasNextPage: boolean; endCursor: string | null };          nodes: { name?: string | null; namespace: string; key: string }[];        };      })?.metafieldDefinitions;      if (!conn) break;      for (const n of conn.nodes) {        const nsKey = `${n.namespace}.${n.key}`;        const nm = (n.name ?? "").trim();        if (nm) {          const had = definitionNameMap.has(nsKey);          definitionNameMap.set(nsKey, nm);          if (!had) metafieldDefinitionEntriesLoaded += 1;        }      }      hasNext = conn.pageInfo.hasNextPage;      after = conn.pageInfo.endCursor;    }  }  if (includeMetafields) {    // Probe any candidate owner types that haven't been checked yet this process.    const unprobed = CANDIDATE_METAFIELD_OWNER_TYPES.filter((ot) => !_ownerTypeProbeCache.has(ot));    if (unprobed.length > 0) {      await Promise.all(        unprobed.map(async (ot) => {          try {            const { data } = await shopifyGqlWithExtensions(domain, token, METAFIELD_DEFINITIONS_QUERY, {              ownerType: ot,              first: 1,              after: null,            });            const supported = !!(data as { metafieldDefinitions?: unknown })?.metafieldDefinitions;            _ownerTypeProbeCache.set(ot, supported);            console.info(`[sync/metafield-owner-probe] ${ot} ΓåÆ ${supported ? "supported" : "no data ΓÇö will still load"}`);          } catch (err) {            _ownerTypeProbeCache.set(ot, false);            console.info(`[sync/metafield-owner-probe] ${ot} ΓåÆ enum_error ΓÇö skipping (${(err instanceof Error ? err.message : String(err)).slice(0, 120)})`);          }        }),      );    }    // Build final list: baseline always included; candidates only if probe passed.    const validCandidates = CANDIDATE_METAFIELD_OWNER_TYPES.filter((ot) => _ownerTypeProbeCache.get(ot) === true);    const ownerTypes: MetafieldOwnerType[] = [...BASELINE_METAFIELD_OWNER_TYPES, ...validCandidates];    if (validCandidates.length > 0) {      console.info(`[sync/metafield-definitions] Loading definitions for ${ownerTypes.length} owner types (${validCandidates.length} new: ${validCandidates.join(", ")})`);    }    await Promise.all(ownerTypes.map(async (ot) => {      try {        await loadMetafieldDefinitionNames(ot);      } catch (err) {        console.warn(`[sync/metafield-definitions] Skipping ${ot} ΓÇö ${(err instanceof Error ? err.message : String(err)).slice(0, 200)}`);      }    }));  }  const productPageSize = includeMetafields ? SYNC_PAGE_SIZE_WITH_METAFIELDS : SYNC_PRODUCTS_PAGE_SIZE;  const productFilter = params.productQueryFilter    ? `updated_at:>=${params.productQueryFilter}`    : "";  const productPageQuery = includeMetafields    ? buildProductsPageWithMetafieldsQuery(productFilter)    : buildProductsPageQuery(productFilter);  // Use count query only when no filter (count query hardcodes the filter string into GQL at build time)  const productsCountQuery = buildProductsCountQuery(productFilter);  // Shared progress state ΓÇö accessible by all sub-pipelines.  let totalCount: number | null = null;  let processed = 0;  const report = () => {    const pct =      totalCount !== null && totalCount > 0        ? Math.min(100, Math.round((processed / totalCount) * 100))        : null;    onProgress?.({ processed, total: totalCount, pct });  };  // ΓöÇΓöÇ Products pipeline (conditional) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ  if (shouldSyncProducts(syncTypes)) {    const countPromise = (async (): Promise<number | null> => {      try {        const { data: countData, extensions: countExt } = await shopifyGqlWithExtensions(          domain, token, productsCountQuery        );        await maybeThrottleAfterGql(countExt);        const c = (countData as { productsCount?: { count?: number } | null })?.productsCount?.count;        if (typeof c === "number" && c >= 0) return c;      } catch { /* optional */ }      return null;    })();    const firstPagePromise = shopifyGqlWithExtensions(domain, token, productPageQuery, {      first: productPageSize,      after: null,    });    const [cnt, firstPage] = await Promise.all([countPromise, firstPagePromise]);    totalCount = cnt;    await maybeThrottleAfterGql(firstPage.extensions);    report();    type ProductNodeMaybeWithMeta = {      id: string; handle: string; title?: string | null; status?: string | null; publishedAt?: string | null;      metafields?: { nodes: DirectMetafield[] };    };    type ProductConn = {      products: {        pageInfo: { hasNextPage: boolean; endCursor: string | null };        edges: { node: ProductNodeMaybeWithMeta }[];      };    };    async function processProductNodes(nodes: ProductNodeMaybeWithMeta[]): Promise<void> {      if (nodes.length === 0) return;      const batchChunks: ProductNodeMaybeWithMeta[][] = [];      for (let i = 0; i < nodes.length; i += SYNC_BATCH_RESOURCE_COUNT)        batchChunks.push(nodes.slice(i, i + SYNC_BATCH_RESOURCE_COUNT));      for (let w = 0; w < batchChunks.length; w += SYNC_PARALLEL_BATCHES) {        const wave = batchChunks.slice(w, w + SYNC_PARALLEL_BATCHES);        const waveResults = await Promise.all(          wave.map(async (chunk) => {            const n = chunk.length;            if (n === 0) return [] as ShopifySyncProductRow[];            const query = buildTranslatableBatchQuery(n);            const variables: Record<string, unknown> = { targetLocale };            chunk.forEach((node, i) => { variables[`id${i}`] = node.id; });            const { data: trData, extensions: trExt } = await shopifyGqlWithExtensions(domain, token, query, variables);            await maybeThrottleAfterGql(trExt);            const d = trData as Record<string, TrNode>;            const rows: ShopifySyncProductRow[] = [];            for (let i = 0; i < chunk.length; i++) {              const node = chunk[i];              const shopify_id = gidToNumericId(node.id);              const tr = d[`r${i}`];              const content = tr?.translatableContent ?? [];              const translations = tr?.translations ?? [];              typeRawBuffer.push({ resourceId: node.id, content, translations });              const { resolved: srcLocale, usedFallback: used_fallback } = resolveSourceLocale(content, sourceLocale);              if (used_fallback) console.warn("LOCALE_FALLBACK", { handle: node.handle, shopify_id, requested_locale: sourceLocale, resolved_locale: srcLocale });              // Merge direct metafield values (when metafield sync enabled) ΓÇö direct values win.              const baseSource = pickFieldsFromTranslatableContent(content, srcLocale);              const sourceFields = node.metafields?.nodes?.length                ? mergeDirectMetafields(                    baseSource,                    node.metafields.nodes,                    "PRODUCT",                    node.title ?? node.handle ?? "",                    definitionNameMap                  )                : baseSource;              const targetFields = fieldsFromTargetTranslationsOnly(translations);              const productRow = toLegacySyncedProductRow(                node.handle, shopify_id,                sourceFields,                targetFields,                used_fallback, srcLocale,                parseProductResourceStatus(node.status ?? undefined),                node.publishedAt ?? null              );              if (includeMetafields && node.metafields?.nodes?.length) {                // Extract metaobject reference GIDs from list.metaobject_reference fields.                const moRefs: string[] = [];                for (const mf of node.metafields.nodes) {                  if (mf.type !== "list.metaobject_reference" && mf.type !== "metaobject_reference") continue;                  if (!mf.value) continue;                  try {                    const gids: string[] = mf.type === "list.metaobject_reference"                      ? (JSON.parse(mf.value) as string[])                      : [mf.value];                    for (const gid of gids) {                      const numId = gidToNumericId(gid);                      if (numId) moRefs.push(numId);                    }                  } catch { /* skip malformed values */ }                }                if (moRefs.length > 0) productRow.metaobjectRefs = moRefs;                const mfRows = createMetafieldRowsFromDirect(                  node.metafields.nodes,                  "PRODUCT",                  node.title ?? node.handle ?? "",                  node.handle,                  srcLocale,                  targetFields.metafields,                  definitionNameMap                );                metafieldRowsCreated += mfRows.length;                rows.push(...mfRows);              }              rows.push(productRow);            }            return rows;          })        );        for (const part of waveResults) {          products.push(...part);          processed += part.length;          report();        }      }    }    let pageData: unknown = firstPage.data;    let cursor: string | null = null;    let hasNext = true;    while (hasNext) {      const conn = (pageData as ProductConn).products;      hasNext = conn.pageInfo.hasNextPage;      cursor = conn.pageInfo.endCursor;      await processProductNodes(conn.edges.map((e) => e.node));      if (!hasNext) break;      const { data: nextData, extensions: nextExt } = await shopifyGqlWithExtensions(domain, token, productPageQuery, {        first: productPageSize, after: cursor,      });      await maybeThrottleAfterGql(nextExt);      pageData = nextData;    }    await fireTypeComplete("PRODUCT");  } // end products pipeline  // ΓöÇΓöÇ Pages pipeline ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ  // ΓöÇΓöÇ Generic pipeline helper (reused by pages, collections, blogs, articles)  async function syncResourceList(    listQuery: string,    connKey: string,    kind: ShopifySyncProductRow["resourceKind"]  ): Promise<void> {    type GenConn = {      [key: string]: {        pageInfo: { hasNextPage: boolean; endCursor: string | null };        edges: { node: { id: string; handle: string } }[];      };    };    const firstData = await shopifyGqlWithExtensions(domain, token, listQuery, {      first: SYNC_PRODUCTS_PAGE_SIZE,      after: null,    });    await maybeThrottleAfterGql(firstData.extensions);    let pageData: unknown = firstData.data;    let cursor: string | null = null;    let hasNext = true;    while (hasNext) {      const conn = (pageData as GenConn)[connKey];      hasNext = conn.pageInfo.hasNextPage;      cursor = conn.pageInfo.endCursor;      const nodes = conn.edges.map((e) => e.node);      if (nodes.length > 0) {        const chunks: typeof nodes[] = [];        for (let i = 0; i < nodes.length; i += SYNC_BATCH_RESOURCE_COUNT)          chunks.push(nodes.slice(i, i + SYNC_BATCH_RESOURCE_COUNT));        for (let w = 0; w < chunks.length; w += SYNC_PARALLEL_BATCHES) {          const wave = chunks.slice(w, w + SYNC_PARALLEL_BATCHES);          const waveResults = await Promise.all(            wave.map(async (chunk) => {              if (!chunk.length) return [] as ShopifySyncProductRow[];              const q = buildTranslatableBatchQuery(chunk.length);              const vars: Record<string, unknown> = { targetLocale };              chunk.forEach((n, i) => { vars[`id${i}`] = n.id; });              const { data: td, extensions: te } = await shopifyGqlWithExtensions(domain, token, q, vars);              await maybeThrottleAfterGql(te);              const d = td as Record<string, TrNode>;              return chunk.map((node, i) => {                const tr = d[`r${i}`];                const content = tr?.translatableContent ?? [];                const translations = tr?.translations ?? [];                typeRawBuffer.push({ resourceId: node.id, content, translations });                const { resolved: srcLocale, usedFallback } = resolveSourceLocale(content, sourceLocale);                return {                  ...toLegacySyncedProductRow(                    node.handle, gidToNumericId(node.id),                    pickFieldsFromTranslatableContent(content, srcLocale),                    fieldsFromTargetTranslationsOnly(translations),                    usedFallback, srcLocale, undefined, undefined                  ),                  resourceKind: kind,                } as ShopifySyncProductRow;              });            })          );          for (const part of waveResults) {            products.push(...part);            processed += part.length;            onProgress?.({ processed, total: totalCount, pct: null });          }        }      }      if (!hasNext) break;      const { data: nd, extensions: ne } = await shopifyGqlWithExtensions(        domain, token, listQuery, { first: SYNC_PRODUCTS_PAGE_SIZE, after: cursor }      );      await maybeThrottleAfterGql(ne);      pageData = nd;    }  }  // ΓöÇΓöÇ Pages / Collections / Blogs / Articles (all reuse helper) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ  // Each is wrapped individually ΓÇö an access-denied error on one type (e.g. pages requires  // read_content scope) must not abort the entire sync.  async function trySyncResourceList(    query: string, connKey: string, kind: ShopifySyncProductRow["resourceKind"]  ) {    try {      await syncResourceList(query, connKey, kind);    } catch (err) {      const msg = err instanceof Error ? err.message : String(err);      console.warn(`[sync] Skipping ${kind} ΓÇö ${msg.slice(0, 200)}`);      typeRawBuffer.splice(0); // discard any partial data on failure    }    await fireTypeComplete(kind as string);  }  if (shouldSyncPages(syncTypes))    await trySyncResourceList(PAGES_PAGE_QUERY,  "pages",   "PAGE");  if (shouldSyncBlogs(syncTypes))    await trySyncResourceList(BLOGS_PAGE_QUERY,  "blogs",   "BLOG");  if (shouldSyncArticles(syncTypes)) await trySyncResourceList(ARTICLES_PAGE_QUERY, "articles", "ARTICLE");  // Collections ΓÇö use metafield-enhanced query when METAFIELD is in types.  if (shouldSyncCollections(syncTypes)) {    if (!includeMetafields) {      await trySyncResourceList(COLLECTIONS_PAGE_QUERY, "collections", "COLLECTION");    } else {      try {        type ColNodeWithMeta = { id: string; handle: string; title?: string | null; metafields?: { nodes: DirectMetafield[] } };        type ColConnWithMeta = { collections: { pageInfo: { hasNextPage: boolean; endCursor: string | null }; edges: { node: ColNodeWithMeta }[] } };        const colPageSize = SYNC_PAGE_SIZE_WITH_METAFIELDS;        let colData = await shopifyGqlWithExtensions(domain, token, COLLECTIONS_PAGE_WITH_METAFIELDS_QUERY, { first: colPageSize, after: null });        await maybeThrottleAfterGql(colData.extensions);        let colHasNext = true; let colCursor: string | null = null; let colPage: unknown = colData.data;        while (colHasNext) {          const conn = (colPage as ColConnWithMeta).collections;          colHasNext = conn.pageInfo.hasNextPage; colCursor = conn.pageInfo.endCursor;          const colNodes = conn.edges.map((e) => e.node);          if (colNodes.length > 0) {            const chunks: ColNodeWithMeta[][] = [];            for (let i = 0; i < colNodes.length; i += SYNC_BATCH_RESOURCE_COUNT) chunks.push(colNodes.slice(i, i + SYNC_BATCH_RESOURCE_COUNT));            for (let w = 0; w < chunks.length; w += SYNC_PARALLEL_BATCHES) {              const wave = chunks.slice(w, w + SYNC_PARALLEL_BATCHES);              const waveResults = await Promise.all(wave.map(async (chunk) => {                if (!chunk.length) return [] as ShopifySyncProductRow[];                const q = buildTranslatableBatchQuery(chunk.length);                const vars: Record<string, unknown> = { targetLocale };                chunk.forEach((n, i) => { vars[`id${i}`] = n.id; });                const { data: td, extensions: te } = await shopifyGqlWithExtensions(domain, token, q, vars);                await maybeThrottleAfterGql(te);                const d = td as Record<string, TrNode>;                return chunk.flatMap((node, i) => {                  const tr = d[`r${i}`];                  const content = tr?.translatableContent ?? [];                  const translations = tr?.translations ?? [];                  typeRawBuffer.push({ resourceId: node.id, content, translations });                  const { resolved: srcLocale, usedFallback } = resolveSourceLocale(content, sourceLocale);                  const baseSource = pickFieldsFromTranslatableContent(content, srcLocale);                  const sourceFields = node.metafields?.nodes?.length                    ? mergeDirectMetafields(                        baseSource,                        node.metafields.nodes,                        "COLLECTION",                        node.title ?? node.handle ?? "",                        definitionNameMap                      )                    : baseSource;                  const targetFields = fieldsFromTargetTranslationsOnly(translations);                  const baseRow: ShopifySyncProductRow = {                    ...toLegacySyncedProductRow(                      node.handle,                      gidToNumericId(node.id),                      sourceFields,                      targetFields,                      usedFallback,                      srcLocale,                      undefined,                      undefined                    ),                    resourceKind: "COLLECTION",                  };                  if (!includeMetafields || !node.metafields?.nodes?.length) return [baseRow];                  // Extract metaobject reference GIDs (same as product pipeline).                  const moRefs: string[] = [];                  for (const mf of node.metafields.nodes) {                    if (mf.type !== "list.metaobject_reference" && mf.type !== "metaobject_reference") continue;                    if (!mf.value) continue;                    try {                      const gids: string[] = mf.type === "list.metaobject_reference"                        ? (JSON.parse(mf.value) as string[])                        : [mf.value];                      for (const gid of gids) {                        const numId = gidToNumericId(gid);                        if (numId) moRefs.push(numId);                      }                    } catch { /* skip malformed */ }                  }                  if (moRefs.length > 0) baseRow.metaobjectRefs = moRefs;                  const mfRows = createMetafieldRowsFromDirect(                    node.metafields.nodes,                    "COLLECTION",                    node.title ?? node.handle ?? "",                    node.handle,                    srcLocale,                    targetFields.metafields,                    definitionNameMap                  );                  metafieldRowsCreated += mfRows.length;                  return [baseRow, ...mfRows];                });              }));              for (const part of waveResults) {                products.push(...part);                processed += part.length;                onProgress?.({ processed, total: totalCount, pct: null });              }            }          }          if (!colHasNext) break;          const { data: nd, extensions: ne } = await shopifyGqlWithExtensions(domain, token, COLLECTIONS_PAGE_WITH_METAFIELDS_QUERY, { first: colPageSize, after: colCursor });          await maybeThrottleAfterGql(ne); colPage = nd;        }      } catch (err) {        console.warn(`[sync] Skipping COLLECTION (metafields) ΓÇö ${(err instanceof Error ? err.message : String(err)).slice(0, 200)}`);        typeRawBuffer.splice(0); // discard partial data on failure      }      await fireTypeComplete("COLLECTION");    }  }  // ΓöÇΓöÇ Metaobjects pipeline ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ  // Discover ΓåÆ filter ΓåÆ fetch field values ΓåÆ merge EN translations.  // Never hardcodes types, field names, or resource IDs.  if (shouldSyncMetaobjects(syncTypes)) {    // Step 1 ΓÇö discover all translatable metaobject types (paginated).    type DefNode = { type: string; capabilities: { translatable: { enabled: boolean } } };    type DefConn = { metaobjectDefinitions?: { pageInfo: { hasNextPage: boolean; endCursor: string | null }; nodes: DefNode[] } };    const translatableTypes: string[] = [];    let defCursor: string | null = null;    let defHasNext = true;    console.info(`[metaobjects/token-check] token=${token.slice(0, 12)}... shop=${domain}`);    try {      while (defHasNext) {        const { data: defData, extensions: defExt } = await shopifyGqlWithExtensions(          domain, token, METAOBJECT_DEFINITIONS_QUERY, { first: 50, after: defCursor }        );        await maybeThrottleAfterGql(defExt);        const defConn = (defData as DefConn)?.metaobjectDefinitions;        if (!defConn) break;        for (const node of defConn.nodes) {          if (node.capabilities?.translatable?.enabled) translatableTypes.push(node.type);        }        defHasNext = defConn.pageInfo.hasNextPage;        defCursor = defConn.pageInfo.endCursor;        if (!defHasNext) break;      }      console.info(`[sync/metaobjects] Found ${translatableTypes.length} translatable type(s):`, translatableTypes);    } catch (err) {      // Surface the exact error ΓÇö permission issues (missing read_metaobjects scope) will be visible here.      console.error("[sync/metaobjects] metaobjectDefinitions failed:", err instanceof Error ? err.message : err);    }    // Step 2 ΓÇö for each translatable type: fetch instances with field values + EN translations.    type MoField = { key: string; value: string | null; type: string };    type MoNode = { id: string; handle: string; fields: MoField[] };    type MoConn = { metaobjects?: { pageInfo: { hasNextPage: boolean; endCursor: string | null }; nodes: MoNode[] } };    for (const moType of translatableTypes) {      try {        let moCursor: string | null = null;        let moHasNext = true;        while (moHasNext) {          const { data: moData, extensions: moExt } = await shopifyGqlWithExtensions(            domain, token, METAOBJECTS_WITH_FIELDS_QUERY,            { type: moType, first: SYNC_PRODUCTS_PAGE_SIZE, after: moCursor }          );          await maybeThrottleAfterGql(moExt);          const moConn = (moData as MoConn)?.metaobjects;          if (!moConn) break;          moHasNext = moConn.pageInfo.hasNextPage;          moCursor = moConn.pageInfo.endCursor;          const instances = moConn.nodes;          if (instances.length > 0) {            const chunks: MoNode[][] = [];            for (let i = 0; i < instances.length; i += SYNC_BATCH_RESOURCE_COUNT)              chunks.push(instances.slice(i, i + SYNC_BATCH_RESOURCE_COUNT));            for (let w = 0; w < chunks.length; w += SYNC_PARALLEL_BATCHES) {              const wave = chunks.slice(w, w + SYNC_PARALLEL_BATCHES);              const waveResults = await Promise.all(                wave.map(async (chunk) => {                  if (!chunk.length) return [] as ShopifySyncProductRow[];                  // Batch-fetch EN translations via translatableResource (reuses existing query).                  const q = buildTranslatableBatchQuery(chunk.length);                  const vars: Record<string, unknown> = { targetLocale };                  chunk.forEach((n, i) => { vars[`id${i}`] = n.id; });                  const { data: trData, extensions: trExt } = await shopifyGqlWithExtensions(domain, token, q, vars);                  await maybeThrottleAfterGql(trExt);                  const trMap = trData as Record<string, TrNode>;                  return chunk                    .map((node, i) => {                      // Source: direct field values ΓÇö no locale matching required.                      const sourceFields: Record<string, { ru_content: string; en_content: string; fieldType: import("@/types").FieldType }> = {};                      for (const f of node.fields) {                        if (!TRANSLATABLE_FIELD_TYPES.has(f.type)) continue;                        if (!f.value) continue;                        sourceFields[f.key] = {                          ru_content: f.value,                          en_content: "",                          fieldType: shopifyTypeToFieldType(f.type.toUpperCase()),                        };                      }                      if (Object.keys(sourceFields).length === 0) return null;                      // Target: merge existing EN translations from translatableResource.                      const trNode = trMap[`r${i}`];                      const translations = trNode?.translations ?? [];                      typeRawBuffer.push({                        resourceId: node.id,                        content: trNode?.translatableContent ?? [],                        translations,                      });                      for (const t of translations) {                        if (t.key in sourceFields && t.value) {                          sourceFields[t.key].en_content = t.value;                        }                      }                      return toMetaobjectSyncRow(                        node.handle, gidToNumericId(node.id), moType,                        sourceFields, false, sourceLocale                      );                    })                    .filter((r): r is ShopifySyncProductRow => r !== null);                })              );              for (const part of waveResults) {                products.push(...part);                processed += part.length;                onProgress?.({ processed, total: totalCount, pct: null });              }            }          }          if (!moHasNext) break;        }      } catch (err) {        console.error(`[sync/metaobjects] type "${moType}" failed:`, err instanceof Error ? err.message : err);      }    }    await fireTypeComplete("METAOBJECT");  }  // ΓöÇΓöÇ Generic translatableResources pipeline for all remaining Shopify types ΓöÇΓöÇ  const alreadyHandled = new Set<ShopifySyncResourceType>([    "PRODUCT",    "COLLECTION",    // canonical 2025-07 names + legacy aliases so both are excluded from generic pipeline    "PAGE",    "ONLINE_STORE_PAGE",    "BLOG",    "ONLINE_STORE_BLOG",    "ARTICLE", "ONLINE_STORE_ARTICLE",    "METAOBJECT",  ]);  const rawRequestedTypes: ShopifySyncResourceType[] = syncTypes;  // LINK (individual navigation links / menu items) must always be synced  // alongside MENU ΓÇö they are parent-child in Shopify's Translations API but  // exposed as two separate TranslatableResourceType enum values.  const requestedTypes: ShopifySyncResourceType[] =    rawRequestedTypes.includes("MENU") && !rawRequestedTypes.includes("LINK")      ? [...rawRequestedTypes, "LINK"]      : rawRequestedTypes;  const genericTypes = requestedTypes.filter((t) => !alreadyHandled.has(t));  // Fetched lazily on first ONLINE_STORE_THEME row.  let themeNameMap: Map<string, string> | null = null;  // Pre-fetch menu tree so MENU and LINK records can use handles / parent context.  let linkParentMap: Map<string, LinkParentInfo> | null = null;  let menuInfoMap:   Map<string, { handle: string; title: string }> | null = null;  if (requestedTypes.includes("MENU") || requestedTypes.includes("LINK")) {    const maps = await fetchMenuMaps(domain, token);    linkParentMap = maps.linkParentMap;    menuInfoMap   = maps.menuInfoMap;  }  // Pre-fetch shop policy titles so SHOP_POLICY nodes display human-readable names  // (e.g. "Refund Policy") instead of the raw GID-based fallback ("shop_policy-123").  let policyTitleMap: Map<string, string> | null = null;  if (requestedTypes.includes("SHOP_POLICY")) {    policyTitleMap = await fetchPolicyTitleMap(domain, token);  }  for (const resourceType of genericTypes) {    // Accumulator for ONLINE_STORE_THEME string filter stats (null for all other types).    const themeStats =      resourceType === "ONLINE_STORE_THEME"        ? { included: 0, excluded: {} as Record<ThemeExclusionReason, number> }        : null;    try {      // Keep progress roughly accurate when count endpoint is available for this type.      if (totalCount === null) {        try {          const { data: countData, extensions: countExt } = await shopifyGqlWithExtensions(            domain,            token,            TRANSLATABLE_RESOURCES_COUNT_QUERY,            { resourceType }          );          await maybeThrottleAfterGql(countExt);          const c = (countData as { translatableResourcesCount?: { count?: number } | null })            ?.translatableResourcesCount?.count;          if (typeof c === "number" && c >= 0) {            totalCount = c;            report();          }        } catch {          // Optional endpoint per-type; ignore if unavailable.        }      }      type GenericNode = {        resourceId: string;        translatableContent: TranslatableContentItem[] | null;        translations: TranslationItem[] | null;      };      type GenericConn = {        translatableResources?: {          pageInfo: { hasNextPage: boolean; endCursor: string | null };          nodes: GenericNode[];        };      };      let after: string | null = null;      let hasNext = true;      while (hasNext) {        const { data: pageData, extensions: pageExt } = await shopifyGqlWithExtensions(          domain,          token,          TRANSLATABLE_RESOURCES_QUERY,          { resourceType, first: SYNC_PRODUCTS_PAGE_SIZE, after, targetLocale }        );        await maybeThrottleAfterGql(pageExt);        const conn = (pageData as GenericConn).translatableResources;        if (!conn) break;        for (const node of conn.nodes) {          const content = node.translatableContent ?? [];          const translations = node.translations ?? [];          if (content.length === 0 && translations.length === 0) continue;          typeRawBuffer.push({ resourceId: node.resourceId, content, translations });          const { resolved: srcLocale, usedFallback } = resolveSourceLocale(content, sourceLocale);          let fields = buildDynamicFieldBag(content, translations, srcLocale);          // Diagnostic: log first 3 option records so we can verify ru_content is populated          if (            (resourceType === "PRODUCT_OPTION" || resourceType === "PRODUCT_OPTION_VALUE") &&            processed < 3          ) {            const firstContent = content[0];            const nameField = fields["name"];            console.info(              `[sync/${resourceType}] record #${processed + 1} ` +              `resourceId=${node.resourceId} ` +              `content[0]={key:${firstContent?.key}, locale:"${firstContent?.locale ?? ""}", value:"${firstContent?.value?.slice(0, 40) ?? ""}"}` +              ` ΓåÆ ru_content="${nameField?.ru_content?.slice(0, 40) ?? "(empty)"}"` +              (nameField?.ru_content ? " Γ£ô" : " Γ£ù MISSING SOURCE")            );          }          // For theme records, strip keys whose source value is not human-readable text          // (colour hex/rgb/hsl, URLs, Liquid snippets, CSS dimensions, booleans,          // numbers, icon classes, image/asset paths, empty strings).          if (resourceType === "ONLINE_STORE_THEME" && themeStats) {            // Single pass: classify every field, keep or discard, accumulate counts.            const kept: [string, typeof fields[string]][] = [];            const nodeExcluded: Partial<Record<ThemeExclusionReason, number>> = {};            for (const [k, fv] of Object.entries(fields)) {              // Key-based check first (Plus-only namespace, store-agnostic).              // Value-based check second (colours, URLs, Liquid, etc.).              const reason =                themePlusOnlyKeyReason(k) ?? themeValueExclusionReason(fv.ru_content);              if (reason) {                nodeExcluded[reason] = (nodeExcluded[reason] ?? 0) + 1;                themeStats.excluded[reason] = (themeStats.excluded[reason] ?? 0) + 1;              } else {                kept.push([k, fv]);                themeStats.included++;              }            }            const nodeExcludedTotal = Object.values(nodeExcluded).reduce((s, n) => s + n, 0);            const reasonSummary = Object.entries(nodeExcluded)              .sort(([, a], [, b]) => b - a)              .map(([r, n]) => `${r}:${n}`)              .join(", ");            console.info(              `[sync/ONLINE_STORE_THEME] resourceId=${node.resourceId} ΓÇö ` +              `${kept.length} strings included, ${nodeExcludedTotal} excluded` +              (reasonSummary ? ` (${reasonSummary})` : ""),            );            fields = Object.fromEntries(kept);            // Log unique section IDs and key prefixes for context-mapping verification.            const sectionIds = new Set<string>();            const keyPrefixes = new Set<string>();            for (const key of Object.keys(fields)) {              const sectionMatch = key.match(/^sections\.([^.]+)\./);              if (sectionMatch) sectionIds.add(sectionMatch[1]);              keyPrefixes.add(key.split(".").slice(0, 2).join("."));            }            console.log(              `[sync/theme] resourceId=${node.resourceId} ΓÇö unique sectionIds (${sectionIds.size}):`,              [...sectionIds].sort().join(", ") || "(none ΓÇö likely locale-file keys)",            );            console.log(              `[sync/theme] key prefixes (${keyPrefixes.size}):`,              [...keyPrefixes].sort().join(", "),            );          }          if (Object.keys(fields).length === 0) continue;          const shopify_id = gidToNumericId(node.resourceId);          // For themes, resolve the human-readable name from the REST themes list.          let themeName: string | null = null;          if (resourceType === "ONLINE_STORE_THEME") {            if (!themeNameMap) {              themeNameMap = await fetchThemeNameMap(domain, token);            }            themeName = themeNameMap.get(shopify_id) ?? null;          }          // For MENU resources, prefer the actual Shopify menu handle (e.g. "main-menu")          // and title from the Navigation API over the generic ID-based fallback.          const menuInfo = resourceType === "MENU" && menuInfoMap            ? menuInfoMap.get(shopify_id)            : undefined;          const handle =            themeName              ? `${themeName.toLowerCase().replace(/[^a-z0-9]+/g, "-")}-${shopify_id}`              : resourceType === "ONLINE_STORE_THEME"                ? `theme-${shopify_id}`                : resourceType === "MENU"                  ? menuInfo?.handle ?? fields.handle?.ru_content?.trim() ?? `menu-${shopify_id}`                  : resourceType === "LINK"                    ? `link-${shopify_id}`                                 // stable ΓÇö link handles not translatable                    : fields.handle?.ru_content?.trim() ||                      fields.title?.ru_content?.trim() ||                      fields.name?.ru_content?.trim() ||                      `${resourceType.toLowerCase()}-${shopify_id}`;          // For LINK items, prefix with the parent menu handle so the table is scannable.          const linkMeta =            resourceType === "LINK" && linkParentMap              ? linkParentMap.get(shopify_id)              : undefined;          const displayTitle =            themeName              ? `Theme: ${themeName}`              : resourceType === "ONLINE_STORE_THEME"                ? `Theme: ${shopify_id}`                : resourceType === "MENU"                  ? menuInfo?.title ?? fields.title?.ru_content ?? fields.name?.ru_content ?? handle                  : resourceType === "LINK" && linkMeta                    ? `${linkMeta.menuTitle || linkMeta.menuHandle} ΓÇ║ ${fields.title?.ru_content ?? fields.name?.ru_content ?? `link-${shopify_id}`}`                    : resourceType === "SHOP_POLICY" && policyTitleMap                      // Try numeric ID first (most robust), fall back to full GID, then field/handle.                      ? policyTitleMap.get(shopify_id) ?? policyTitleMap.get(node.resourceId) ?? fields.title?.ru_content ?? handle                      : fields.title?.ru_content ?? fields.name?.ru_content ?? handle;          if (resourceType === "ONLINE_STORE_THEME") {            console.log(`[sync/theme] shopify_id=${shopify_id} themeName=${themeName ?? "(none)"} handle=${handle} displayTitle=${displayTitle}`);          }          if (resourceType === "SHOP_POLICY") {            const byNumeric = policyTitleMap?.get(shopify_id);            const byFullGid = policyTitleMap?.get(node.resourceId);            console.info(              `[sync/SHOP_POLICY] node resourceId="${node.resourceId}" shopify_id="${shopify_id}" ` +              `ΓåÆ byNumeric="${byNumeric ?? "(miss)"}" byFullGid="${byFullGid ?? "(miss)"}" ` +              `ΓåÆ displayTitle="${displayTitle}"`,            );          }          products.push({            fields,            handle,            shopify_id,            ru_title: displayTitle,            ru_body: fields.body_html?.ru_content ?? fields.body?.ru_content ?? "",            en_title: fields.title?.en_content ?? fields.name?.en_content ?? "",            en_body: fields.body_html?.en_content ?? fields.body?.en_content ?? "",            ru_meta_title: fields.meta_title?.ru_content ?? "",            en_meta_title: fields.meta_title?.en_content ?? "",            ru_meta_description: fields.meta_description?.ru_content ?? "",            en_meta_description: fields.meta_description?.en_content ?? "",            used_fallback: usedFallback,            resolved_source_locale: srcLocale,            fallback_reason: usedFallback ? "missing_source_locale" : null,            resourceKind: resourceType,            ...(linkMeta              ? {                  parentMenuId:   linkMeta.menuId,                  parentMenuHandle: linkMeta.menuHandle,                  menuItemDepth:  linkMeta.depth,                  parentLinkId:   linkMeta.parentLinkId,                  itemIndex:      linkMeta.index,                }              : {}),            ...(menuInfo              ? {                  menuHandle: menuInfo.handle,                  menuTitle:  menuInfo.title,                }              : {}),          });          processed += 1;          report();        }        hasNext = conn.pageInfo.hasNextPage;        after = conn.pageInfo.endCursor;      }      // After all pages for ONLINE_STORE_THEME, log the sync-level totals.      if (themeStats) {        const totalExcluded = Object.values(themeStats.excluded).reduce((s, n) => s + n, 0);        const reasonSummary = Object.entries(themeStats.excluded)          .sort(([, a], [, b]) => b - a)          .map(([r, n]) => `${r}:${n}`)          .join(", ");        console.info(          `[sync/ONLINE_STORE_THEME] sync complete ΓÇö ` +          `${themeStats.included} strings included, ${totalExcluded} excluded` +          (reasonSummary ? ` (${reasonSummary})` : ""),        );      }    } catch (err) {      const msg = err instanceof Error ? err.message : String(err);      const isEnumErr = /argument|enum|invalid value/i.test(msg);      if (isEnumErr) {        console.warn(`[sync] ${resourceType} is not a valid TranslatableResourceType on API ${API_VERSION} ΓÇö skipping.`);      } else {        console.warn(`[sync] Skipping ${resourceType} ΓÇö ${msg.slice(0, 200)}`);      }      typeRawBuffer.splice(0); // discard partial data on failure    }    await fireTypeComplete(resourceType);  }  const finalTotal = totalCount ?? products.length;  const finalPct =    totalCount !== null && totalCount > 0      ? Math.min(100, Math.round((products.length / totalCount) * 100))      : 100;  onProgress?.({    processed: products.length,    total: finalTotal,    pct: finalPct,  });  if (includeMetafields) {    console.info("[sync/metafields] summary", {      definitionEntriesLoaded: metafieldDefinitionEntriesLoaded,      definitionMapSize: definitionNameMap.size,      metafieldRowsCreated,    });  }  return products;}