// ΓöÇΓöÇ Raw CSV row (Shopify Translate & Adapt export format) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇexport interface RawTranslationRow {  Type: string;  Identification: string;  Field: string;  Locale: string;  Market: string;  Status: string;  "Default content": string;  "Translated content": string;  [key: string]: string;}// ΓöÇΓöÇ Field-level translation ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇexport type FieldType = "plain" | "html" | "seo_title" | "seo_desc";export type FieldStatus = "done" | "outdated" | "missing";export interface TranslationField {  field: string;  fieldType: FieldType;  ru_content: string;       // Default content (source)  en_content: string;       // Translated content (editable)  csvStatus: string;        // original Status from CSV  status: FieldStatus;  /** Optional Shopify metafield metadata for better labels in UI. */  displayName?: string;  namespaceKey?: string;  metafieldId?: string;  parentTitle?: string;  parentType?: string;}// ΓöÇΓöÇ Record (grouped by Type + Identification + Locale) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇexport type ContentType = "PRODUCT" | "COLLECTION" | "PAGE" | "ARTICLE" | "BLOG" | string;/** Shopify Admin resource status (products, etc.) when loaded from API sync. */export type ShopifyResourceStatus = "ACTIVE" | "DRAFT" | "ARCHIVED";export type RecordStatus = "new" | "partial" | "outdated" | "translated";/** Result of bulk/single translate from `TranslatorApp` `handleTranslate`. */export type TranslateRunResult = "ok" | "skipped" | "not_found" | "error";/** EN coverage for title + body (`en_title` / `en_body`). */export type TranslationStatus = "missing" | "partial" | "complete";export interface TranslationRecord {  id: string;               // `${type}__${identification}__${locale}`  type: ContentType;  identification: string;  handle: string;  locale: string;  fields: TranslationField[];  status: RecordStatus;  translation_status: TranslationStatus;  isTranslating?: boolean;  translatedAt?: number;        // ms timestamp of last translation in this session  /** From POST /api/shopify/sync when configured source locale was missing for title/body. */  used_fallback?: boolean;  /** Actual locale used for source (RU) fields when pulled from Admin API. */  resolved_source_locale?: string;  /**   * Russian title snapshot aligned with the current English title (save/translate/sync).   * If `title.ru_content` differs from this after a sync, English is marked outdated.   */  sourceTitleAtSync?: string;  /** User-marked reviewed in this session (bulk / QA workflow). */  reviewed?: boolean;  /** From Shopify Admin (e.g. product Active / Draft / Archived) when known. */  shopifyResourceStatus?: ShopifyResourceStatus;  /** ISO datetime from Admin API; used when `shopifyResourceStatus` is absent (e.g. fallback Active/Draft). */  shopifyPublishedAt?: string | null;  /**   * Numeric IDs (extracted from GIDs) of Metaobject entries referenced by this product   * via `list.metaobject_reference` metafields. Populated only when Metafields sync is enabled.   * Used to embed FAQ / other structured metaobject content inside the product editor.   */  metaobjectRefs?: string[];  /**   * Menu parent linkage ΓÇö populated for LINK records only.   * `parentMenuId` is the numeric Shopify ID of the MENU that owns this link.   * `parentMenuHandle` is the human-readable handle (e.g. "main-menu").   * `menuItemDepth` is 0 for top-level items, 1 for sub-items, etc.   * `parentLinkId` is the numeric ID of the parent MenuItem (for nested items).   * `itemIndex` is the 0-based position within the parent's item list (for ordering).   */  parentMenuId?: string;  parentMenuHandle?: string;  menuItemDepth?: number;  parentLinkId?: string;  itemIndex?: number;  /** Populated for MENU records ΓÇö the actual Shopify menu handle and title. */  menuHandle?: string;  menuTitle?: string;}/** * Resource kinds for POST /api/shopify/sync `type`. * * Canonical names match the Shopify Admin GraphQL TranslatableResourceType enum * on API version 2025-07.  Legacy aliases below are accepted by the parser and * normalised to their canonical equivalent before any API call. * * Canonical (2025-07)       Legacy alias (old stored values) * ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ     ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ * PAGE                   ΓåÉ  ONLINE_STORE_PAGE * ARTICLE                ΓåÉ  ONLINE_STORE_ARTICLE * BLOG                   ΓåÉ  ONLINE_STORE_BLOG * SHOP_SMS_TEMPLATE      ΓåÉ  SMS_TEMPLATE (if valid; probed at runtime) */export type ShopifySyncResourceType =  // ΓöÇΓöÇ Canonical 2025-07 enum values ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ  | "PRODUCT"  | "PRODUCT_OPTION"  | "PRODUCT_OPTION_VALUE"  | "COLLECTION"  | "METAFIELD"  | "METAOBJECT"  | "PAGE"  | "ARTICLE"  | "BLOG"  | "ONLINE_STORE_THEME"  | "MENU"  | "LINK"  | "SHOP"  | "SHOP_POLICY"  | "DELIVERY_METHOD_DEFINITION"  | "EMAIL_TEMPLATE"  // SMS_TEMPLATE / SHOP_SMS_TEMPLATE both confirmed invalid on 2025-07 ΓÇö removed  | "PAYMENT_GATEWAY"  | "PACKING_SLIP_TEMPLATE"  | "SELLING_PLAN"  | "SELLING_PLAN_GROUP"  | "FILTER"  // ΓöÇΓöÇ Legacy aliases kept for parse-compat with older stored values ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ  | "ONLINE_STORE_PAGE"    // ΓåÆ PAGE  | "ONLINE_STORE_ARTICLE" // ΓåÆ ARTICLE  | "ONLINE_STORE_BLOG"    // ΓåÆ BLOG  | "ONLINE_STORE_MENU"    // ΓåÆ MENU  | "SMS_TEMPLATE"         // kept for parse-compat only; invalid on 2025-07  // PRODUCT_VARIANT kept for parse-compat only; not a valid TranslatableResourceType on 2025-07  | "PRODUCT_VARIANT";/** Row shape returned by POST /api/shopify/sync (products, pages, collections, blogs, articles). */export interface ShopifySyncProductRow {  /** Which Shopify resource type this row represents. Defaults to PRODUCT if absent. */  resourceKind?: string;  /**   * Dynamic field bag for future resources/metafields.   * Key = internal field name (e.g. "title", "meta.custom.some_key"), value = source/target content.   * Legacy `ru_*` / `en_*` properties remain for backward compatibility.   */  fields?: Record<    string,    {      ru_content: string;      en_content: string;      fieldType?: FieldType;      /** Optional Shopify metafield metadata. */      displayName?: string;      namespaceKey?: string;      metafieldId?: string;      parentTitle?: string;      parentType?: string;    }  >;  handle: string;  shopify_id: string;  ru_title: string;  ru_body: string;  en_title: string;  en_body: string;  /** SEO / meta title in source locale (may be empty if not set in Shopify). */  ru_meta_title?: string;  en_meta_title?: string;  ru_meta_description?: string;  en_meta_description?: string;  /** Product type (may be empty if not set as a translatable field in Shopify). */  ru_product_type?: string;  en_product_type?: string;  used_fallback: boolean;  resolved_source_locale: string;  fallback_reason: "missing_source_locale" | null;  /** Product status from Admin API (`ACTIVE` = published, `DRAFT`, `ARCHIVED`). */  resourceStatus?: ShopifyResourceStatus;  /** Product `publishedAt` from Admin API (null = never published). */  publishedAt?: string | null;  /**   * Numeric IDs of Metaobject entries this product references via   * `list.metaobject_reference` metafields (populated when Metafields sync is on).   */  metaobjectRefs?: string[];  /** Menu parent linkage for LINK records (see TranslationRecord above). */  parentMenuId?: string;  parentMenuHandle?: string;  menuItemDepth?: number;  parentLinkId?: string;  itemIndex?: number;  /** Shopify handle and title for MENU records. */  menuHandle?: string;  menuTitle?: string;}// ΓöÇΓöÇ Client settings (localStorage ΓÇö see lib/settingsStorage) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇexport interface AppSettings {  model: string;  systemPrompt: string;  /** Optional glossary rules, one per line: "╨Ü╨╕╤Ç╨╕╨╡╤ê╨║╨╕ = Kirieshki" */  glossary: string;  /** BCP-47 / ISO-639-1 locale code for the source language. Default: "ru" */  sourceLanguage: string;  /** BCP-47 / ISO-639-1 locale code for the target language. Default: "en" */  targetLanguage: string;  shopifyDomain: string;  shopifyClientId: string;  shopifyClientSecret: string;  shopifyAccessToken: string;  shopifyTokenExpiry: number;}// ΓöÇΓöÇ Translate API types ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇexport interface TranslateRequestField {  key: string;  text: string;  fieldType: FieldType;}export interface TranslateResponseField {  key: string;  translation: string;  /** Set to "parse_failed" when OpenAI returned unparseable JSON and source text was used as fallback. */  error?: string;}// ΓöÇΓöÇ CSV column order (must match Shopify's expected format) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇexport const CSV_COLUMNS: (keyof RawTranslationRow)[] = [  "Type",  "Identification",  "Field",  "Locale",  "Market",  "Status",  "Default content",  "Translated content",];